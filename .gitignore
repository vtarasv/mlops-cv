# Secrets: only the committed examples and the stack env files are tracked
.env*
!.env.example
!docker-compose/.env.mlflow
!docker-compose/.env.airflow
!docker-compose/.env.streaming
!docker-compose/.env.serving
*.tfstate
*.tfstate.*
*.tfvars

# Local-only agent workspace: specs, tickets, agent-skill config
.scratch/

# Data, weights, and training outputs
*.pt
/data/
/runs/

# Python
__pycache__/
*.py[codz]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints
